{% extends 'base.html.twig' %}

{% block title %}Country{% endblock %}

{% block body %}
    <h1>Country</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ country.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ country.name }}</td>
            </tr>
            <tr>
                <th>GoldMedalAmount</th>
                <td>{{ country.goldMedalAmount }}</td>
            </tr>
            <tr>
                <th>SilverMedalAmount</th>
                <td>{{ country.silverMedalAmount }}</td>
            </tr>
            <tr>
                <th>BronzeMedalAmount</th>
                <td>{{ country.bronzeMedalAmount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('country_index') }}">back to list</a>

    <a href="{{ path('country_edit', {'id': country.id}) }}">edit</a>

    {{ include('country/_delete_form.html.twig') }}
{% endblock %}
